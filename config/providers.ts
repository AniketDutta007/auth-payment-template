import { FcGoogle } from 'react-icons/fc';
import { IconType } from 'react-icons/lib';
export const PROVIDERS: string[] = ['google'];
export const PROVIDER_ICON: {
	[key: string]: IconType;
} = {
	google: FcGoogle,
};
